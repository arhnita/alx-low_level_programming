#include <stdio.h>
/**
 * main - Entry point
 * description: alphabets
 * Return: always 0 (success)
 */
int main(void)
{
	int a;
	a = 97;
	while (a <= 122)
	putchar(a);
	a++;
	putchar('\n');
	return (0);
}
